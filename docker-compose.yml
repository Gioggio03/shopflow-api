services:
  db:
    image: postgres:16
    container_name: shopflow-db
    environment:
      POSTGRES_DB: shopflow
      POSTGRES_USER: shopflow
      POSTGRES_PASSWORD: shopflow
    ports:
      - "5432:5432"
    volumes:
      - shopflow_data:/var/lib/postgresql/data

volumes:
  shopflow_data: